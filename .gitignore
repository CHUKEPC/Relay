node_modules/
out/
dist/
release/
.npmcache/
.eslintcache
*.log
.DS_Store
.vite/
stats.html

# Secrets & local env overrides — never commit
.env
.env.*
*.local
coverage/

# Build instructions / Claude config — kept private (intentional)
.claude
CLAUDE.md

# QA test kit — local testing material, not part of the shipped source
qa/
chat/
